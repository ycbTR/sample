<%- model_class = Plant -%>
<div class="page-header">
  <h1><%= t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
  <%= link_to t('.new', :default => t("helpers.links.new")),
              new_admin_plant_path,
              :class => 'btn btn-primary pull-right' %>
  <div class="clearfix"></div>

  <%= render partial: "shared/advanced_search" %>

</div>

<table class="table table-striped">
  <thead>
  <tr>
    <th><%= model_class.human_attribute_name(:species) %></th>
    <th><%= model_class.human_attribute_name(:common_name) %></th>
    <th><%= model_class.human_attribute_name(:price_paid) %></th>
    <th><%= model_class.human_attribute_name(:price_charged) %></th>
    <th><%= t '.actions', :default => t("helpers.actions") %></th>
  </tr>
  </thead>
  <tbody>
  <% @plants.each do |plant| %>
      <tr>
        <td><%= plant.species %></td>
        <td><%= plant.common_name %></td>
        <td><%= plant.price_paid %></td>
        <td><%= plant.price_charged %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_admin_plant_path(plant), :class => 'btn btn-mini btn-primary' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      admin_plant_path(plant),
                      :method => :delete,
                      :data => {:confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?'))},
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
  <% end %>
  </tbody>
</table>

<%= paginate @plants %>
