<div class="page-header">
  <h1><%= "Edit #{@type}" %></h1>
</div>
<%= form_for [:admin, @object], url: admin_person_path, :html => {:class => 'form-horizontal'} do |f| %>
    <%= render :partial => 'form', locals: {f: f} %>
<% end %>
