<%= form_for(@lot_number) do |f| %>
  <% if @lot_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lot_number.errors.count, "error") %> prohibited this lot_number from being saved:</h2>

      <ul>
      <% @lot_number.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br />
    <%= f.text_field :number %>
  </div>
  <div class="field">
    <%= f.label :region %><br />
    <%= f.text_field :region %>
  </div>
  <div class="field">
    <%= f.label :provenance %><br />
    <%= f.text_field :provenance %>
  </div>
  <div class="field">
    <%= f.label :location %><br />
    <%= f.text_field :location %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
